import type { FPNumber } from '@sora-substrate/math';
import type { OrderBookStatus } from './consts';
export declare type OrderBookId = {
    dexId: number;
    base: string;
    quote: string;
};
export declare type OrderBook = {
    orderBookId: OrderBookId;
    status: OrderBookStatus;
    lastOrderId: number;
    tickSize: FPNumber;
    stepLotSize: FPNumber;
    minLotSize: FPNumber;
    maxLotSize: FPNumber;
};
/**
 * Bid or Ask of Order Book.
 * Represented as [price, amount]
 * */
export declare type OrderBookPriceVolume = [FPNumber, FPNumber];
export declare type OrderBookAggregated = OrderBook & {
    aggregated: {
        asks: Array<OrderBookPriceVolume>;
        bids: Array<OrderBookPriceVolume>;
    };
};
