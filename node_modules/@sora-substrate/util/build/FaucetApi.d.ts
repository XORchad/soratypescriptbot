import { NumberLike } from '@sora-substrate/math';
import { BaseApi } from './BaseApi';
/**
 * Contains all necessary data and functions for the faucet
 */
export declare class FaucetApi<T = void> extends BaseApi<T> {
    private readonly fausetSignerSeed;
    private readonly faucetSignerName;
    private readonly faucetSignerPassword;
    private faucetSigner?;
    constructor(withKeyringLoading?: boolean);
    /**
     * **NOT USED** will be removed soon, seems that we don't need it
     * @param assetAddress
     * @param accountAddress
     * @returns balance = value * 10 ^ decimals
     */
    getBalance(assetAddress: string, accountAddress: string): Promise<string>;
    send(assetAddress: string, accountAddress: string, amount: NumberLike): Promise<T>;
}
