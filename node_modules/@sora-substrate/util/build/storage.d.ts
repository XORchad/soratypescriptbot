export declare class Storage<T = string> {
    protected namespace: string;
    constructor(namespace?: string);
    all(): Array<Array<any>>;
    get(key: T): string;
    set(key: T, value: any): void;
    remove(key: T): void;
    clear(): void;
}
export declare class AccountStorage<T = string> extends Storage<T> {
    constructor(identity: string);
}
